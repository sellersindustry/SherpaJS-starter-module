{
	"name": "template_name",
	"version": "0.0.2",
	"private": true,
	"scripts": {
		"build": "sherpa build -b Vercel",
		"dev": "sherpa build -b Local && node ./.sherpa/index.js"
	},
	"dependencies": {
		"sherpa-core": "^1.0.2"
	}
}
